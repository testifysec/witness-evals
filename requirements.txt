# Core dependencies for fine-tuning on macOS (Apple Silicon)
mlx>=0.18.0
mlx-lm>=0.18.0
transformers>=4.45.0
datasets>=2.14.0
huggingface-hub>=0.19.0

# Optional: For Hugging Face training (requires more setup)
# torch>=2.0.0
# accelerate>=0.20.0
# peft>=0.5.0
# bitsandbytes>=0.41.0
# trl>=0.7.0
